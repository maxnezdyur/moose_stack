# D011 nominal-baseline

Tested: the frozen constraints reproduce the paper's 1 Hz phase along the scan line.
Showed: 0.0021 rad rms against `provenance/paper_phase_1hz.csv`, five times below the 0.010 rad
noise floor. This run is the baseline for every `phase_shift_*` measure. Feeds F1.
